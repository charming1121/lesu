# GitHub Pages 404 错误排查步骤

## 🔍 请先告诉我以下信息：

1. **您的 GitHub Pages 访问地址是什么？**
   - 格式可能是：`https://username.github.io/repo-name/`
   - 或者：`https://username.github.io/`

2. **您是如何部署的？**
   - 使用 `npm run deploy` (gh-pages)
   - 还是使用 GitHub Actions

## 🛠️ 临时解决方案（请先尝试）

### 步骤 1：重新构建
```bash
npm run build
```

### 步骤 2：检查构建产物
打开 `dist/index.html`，查看资源路径：
- ✅ 正确：`./assets/index-xxx.js` 或 `/repo-name/assets/index-xxx.js`
- ❌ 错误：`/assets/index-xxx.js` (缺少前缀)

### 步骤 3：根据您的地址类型修改配置

**如果地址是** `https://username.github.io/repo-name/`：

修改 `vite.config.js`：
```javascript
base: '/repo-name/', // 替换为您的实际仓库名称
```

**如果地址是** `https://username.github.io/`：

修改 `vite.config.js`：
```javascript
base: '/',
```

### 步骤 4：重新部署
```bash
npm run build
npm run deploy
```

### 步骤 5：清除缓存
- 按 `Ctrl+Shift+R` 强制刷新
- 或使用无痕模式访问

## 📋 常见问题

### Q: 为什么相对路径不工作？
A: GitHub Pages 在某些情况下（特别是子路径部署）需要绝对路径。

### Q: 如何确认我的仓库名称？
A: 查看 GitHub 仓库 URL，例如：
- `https://github.com/username/repo-name` → base 应该是 `/repo-name/`

### Q: 部署后多久生效？
A: 通常 2-5 分钟，有时需要更长时间。

---

**请告诉我您的 GitHub Pages 地址，我会帮您精确配置！**
